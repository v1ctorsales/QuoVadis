import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant="h4">
      Você está na página Home.
    </Typography>
  );
};

export default Home;
